services:
  ruby:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
      - bundle_cache:/usr/local/bundle
    stdin_open: true
    tty: true
